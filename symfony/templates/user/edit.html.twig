

{% block body %}
    <h1>Mise à jour du profil</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_show', {'id': app.user.id}) }}">Retour au profil</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
