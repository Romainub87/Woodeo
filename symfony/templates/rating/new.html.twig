{#% extends 'base.html.twig' %#}

{% block stylesheets %}
{{ encore_entry_link_tags('app') }}
{% endblock %}

{% block body %}
<div class="bg-transparent">
    <h1 class="text-3xl text-white">Create new Rating</h1>

    
    {{ form_start(form) }}
    <div class="text-red-700">
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    </div>
    {{ form_end(form) }}

</div>
{% endblock %}
