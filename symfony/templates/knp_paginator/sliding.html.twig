{# default Sliding pagination control implementation #}
{% if pageCount > 1 %}
    <div class="pagination flex justify-around items-center mt-2">
        {% if first is defined and current != first %}
            <span class="first">
                <a href="{{ path(route, query|merge({(pageParameterName): first})) }}"><img class="hover:scale-110 rotate-180" width="50px" src="{{ asset('img/bouton_next_next.png') }}" ></img></a>
            </span>
        {% else %}
            <span class="previous">
                <img class="opacity-20 rotate-180" width="50px" src="{{ asset('img/bouton_next_next.png') }}"></img>
            </span>
        {% endif %}

        {% if previous is defined %}
            <span class="previous">
                <a rel="prev" href="{{ path(route, query|merge({(pageParameterName): previous})) }}"><img class="hover:scale-110 rotate-180" width="40px" src="{{ asset('img/bouton_next.png') }}"></img></a>
            </span>
        {% else %}
            <span class="previous">
                <img class="opacity-20 rotate-180" width="40px" src="{{ asset('img/bouton_next.png') }}"></img>
            </span>
        {% endif %}
            <div class="flex text-3xl text-white items-center h-full">
        {% for page in pagesInRange %}
            {% if page != current %}
                <span class="page items-center mx-5">
                    <a href="{{ path(route, query|merge({(pageParameterName): page})) }}">{{ page }}</a>
                </span>
            {% else %}
                <span class="current mx-5 border-2 rounded-2xl p-1 px-2">{{ page }}</span>
            {% endif %}
        {% endfor %}
            </div>

        {% if next is defined %}
            <span class="next">
                <a rel="next" href="{{ path(route, query|merge({(pageParameterName): next})) }}"><img class="hover:scale-110" width="40px" src="{{ asset('img/bouton_next.png') }}"></img></a>
            </span>
        {% else %}
            <span class="next">
                <img class="opacity-20" width="40px" src="{{ asset('img/bouton_next.png') }}"></img>
            </span>
        {% endif %}

        {% if last is defined and current != last %}
            <span class="last">
                <a href="{{ path(route, query|merge({(pageParameterName): last})) }}"><img class="hover:scale-110" width="50px" src="{{ asset('img/bouton_next_next.png') }}"></img></a>
            </span>
        {% else %}
            <span class="next">
                <img class="opacity-20" width="40px" src="{{ asset('img/bouton_next_next.png') }}" ></img>
            </span>
        {% endif %}
    </div>
{% endif %}
