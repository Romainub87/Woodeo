<form method="post" action="{{ path('app_series_delete', {'id': series.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ series.id) }}">
    <button class="bg-red-700 px-4 p-1 rounded">Supprimer la série</button>
</form>
