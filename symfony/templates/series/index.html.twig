{% extends 'base.html.twig' %}

{% block title %}Liste séries{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Sofia|Bahnschrift">
{{ encore_entry_link_tags('app') }}
    {% endblock %}

{% block body %}
        <div class="pagination-h">
            {{ knp_pagination_render(series)}}
        </div>

        <div class="flex flex-wrap flex-row w-full items-center justify-center mb-5">
        {% for series in series %}
            <div class="border-white border-8 rounded-3xl flex flex-col-reverse h-auto mx-10 mt-10 items-center">
     
                <h3 class="titre w-full titre bg-white font-bold text-3xl text-center pt-5 pb-4">{{ series.title }}</h3>
                
                <div class="poster w-full"><a href="{{ path('app_series_show', {'id': series.id})}}">
                    <img class="border-none rounded-b-none rounded-2xl h-full w-full" src="{{ series.displayPoster }}" alt="poster">
                </a>
                </div>
            
            </div>
        
        {% endfor %}
        
        </div>

        <div class="pagination-b">
            {{ knp_pagination_render(series)}}
        </div>



{% endblock %}
